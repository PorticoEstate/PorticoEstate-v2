document.addEventListener('DOMContentLoaded', function () {
	const app = document.getElementById('messenger-reply-app');
	const form = document.getElementById('messenger-reply-form');
	fetch(app.dataset.apiUrl).then(response => response.json()).then(message => { document.getElementById('messenger-reply-to').textContent = message.from || ''; document.getElementById('messenger-reply-recipient-id').value = message.from_id || ''; document.getElementById('messenger-reply-subject').value = 'RE: ' + (message.subject || ''); document.getElementById('messenger-reply-content').value = message.content || ''; });
	form.addEventListener('submit', function (event) { event.preventDefault(); const payload = Object.fromEntries(new FormData(form)); payload.csrf_name = app.dataset.csrfName; payload.csrf_value = app.dataset.csrfValue; fetch(app.dataset.actionUrl, {method: 'POST', headers: {'Content-Type': 'application/json', 'csrf_name': app.dataset.csrfName, 'csrf_value': app.dataset.csrfValue}, body: JSON.stringify(payload)}).then(response => response.json().then(data => ({ok: response.ok, data: data}))).then(result => { if (!result.ok) { const error = document.getElementById('messenger-reply-error'); error.textContent = (result.data.errors || [result.data.error]).join('\n'); error.hidden = false; return; } window.location.href = app.dataset.backUrl; }); });
});
