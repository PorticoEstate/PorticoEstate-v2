// Shim for @/app/i18n/settings — used in widget builds only.
export const fallbackLng = {key: 'no', label: 'Bokmal', countryCode: 'no'};
export const languages = [fallbackLng];
export const defaultNS = 'translation';
export const cookieName = 'selected_lang';
