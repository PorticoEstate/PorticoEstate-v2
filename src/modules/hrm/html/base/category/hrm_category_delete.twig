{% extends layout|default('@views/_bare.twig') %}

{% block title %}{{ lang('delete') }} {{ lang(type) }} {{ lang('category') }}{% endblock %}

{% block styles %}
	<style>
		.hrm-category-delete { max-width: 42rem; margin: 0 auto; }
		.hrm-category-delete__panel { padding: 1.25rem; border: 1px solid #d5dde6; border-left: .45rem solid #9f1239; border-radius: 6px; background: #fff; }
		.hrm-category-delete__panel h1 { margin: 0 0 .75rem; font-size: 1.5rem; }
		.hrm-category-delete__summary { display: grid; gap: .4rem; margin: 1rem 0; color: #17324d; }
		.hrm-category-delete__label { color: #526579; font-weight: 600; }
		.hrm-category-delete__actions { display: flex; flex-wrap: wrap; gap: .75rem; margin-top: 1rem; }
		.hrm-category-delete__button { border: 1px solid #24527a; border-radius: 4px; padding: .45rem .85rem; background: #24527a; color: #fff; text-decoration: none; cursor: pointer; }
		.hrm-category-delete__button--danger { border-color: #9f1239; background: #9f1239; }
		.hrm-category-delete__button--secondary { background: #fff; color: #24527a; }
	</style>
{% endblock %}

{% block content %}
	<div class="hrm-category-delete">
		<div class="hrm-category-delete__panel">
			<h1>{{ lang('delete') }} {{ lang(type) }} {{ lang('category') }}</h1>
			<p>{{ lang('do you really want to delete this entry') }}</p>
			<div class="hrm-category-delete__summary">
				<div><span class="hrm-category-delete__label">{{ lang('category id') }}:</span> {{ category_id }}</div>
				<div><span class="hrm-category-delete__label">{{ lang('Descr') }}:</span> {{ values.descr|default('') }}</div>
			</div>
			<form method="post" action="{{ form_action|e }}">
				<div class="hrm-category-delete__actions">
					<button class="hrm-category-delete__button hrm-category-delete__button--danger" type="submit" name="confirm" value="1">{{ lang('yes') }}</button>
					<a class="hrm-category-delete__button hrm-category-delete__button--secondary" href="{{ list_url|e }}">{{ lang('no') }}</a>
				</div>
			</form>
		</div>
	</div>
{% endblock %}